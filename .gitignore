# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example

# Claude Code 개인 설정
CLAUDE.local.md

# 작업 중 참고용 스크린샷 등을 주고받는 임시 폴더 (프로젝트 산출물 아님).
# 최상위로 한정한다 — 앞의 /가 없으면 모든 깊이에서 걸려서
# design-handoff/ref/(레퍼런스 실화면 캡처, 디자인 판단의 근거)까지 같이 빠진다.
/ref/

# 대화용 진행 스크린샷. 코드와 함께 관리할 이유가 없고 계속 쌓이기만 한다.
progress/

# 이 프로젝트는 pnpm을 쓴다. npx 실행 등으로 생기는 npm 락파일은 커밋하지 않는다.
package-lock.json

# Edge Function 타입체크(deno check)를 임시로 돌릴 때 생기는 파일
deno.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
storybook-static
