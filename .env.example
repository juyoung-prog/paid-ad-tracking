# Supabase 프로젝트 생성 후 실제 값으로 채운다 (supabase.com 대시보드 → Project Settings → API).
# anon key만 여기 둔다 — service_role key는 절대 프론트/이 파일에 두지 않는다
# (service_role은 Supabase Edge Function 환경에만: `supabase secrets set`).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
